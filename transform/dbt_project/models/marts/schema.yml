version: 2

models:
  - name: daily_weather_summary
    description: One row per day, aggregated from stg_weather.
    columns:
      - name: day
        data_tests:
          - not_null
          - unique
      - name: reading_count
        data_tests:
          - not_null
